<project basedir="." default="all" name="herickson3"> 

	<!-- everything uses the init target -->
	<target name="init"> 
		<!--  these are the directories that already exist -->
		<property name="src.dir" value="src"/>
		<property name="lib.dir" value="lib"/>
		<property name="main-class" value="edu.gatech.oad.antlab.pkg1.AntLabMain"/>
		
		<!-- these are the directories being created -->
		<property name="build.dir" value="build"/>
		<property name="classes.dir" value="${build.dir}/classes"/>
		<property name="apidoc.dir" value="apidocs"/>
		<property name="jar.dir" value="${build.dir}/jar"/>
		
		<property name="jar.file" value="M4run"/>
		
	</target> 
	
	<!-- prepare is a prereq for most tasks except cleanup -->
	<target name="prepare" depends="init"> 
		<mkdir dir="${build.dir}"/> 
		<mkdir dir="${classes.dir}"/>
		<mkdir dir="${apidoc.dir}"/>
		<mkdir dir="${jar.dir}"/>
	</target> 
	
	<target name="javadoc" depends="prepare">
		<javadoc sourcepath="${src.dir}" destdir="${apidoc.dir}"/>
	</target>
	
	<!-- compile the code -->
	<target name="compile" depends="prepare"> 
		<javac destdir="${classes.dir}" srcdir="${src.dir}" includeantruntime="false">
			<classpath>
				<fileset dir="${lib.dir}/resources.jar">
        			<include name="**/*.jar"/>
      			</fileset>
			</classpath> 
		</javac> 
	</target> 
	
	<target name="jar" depends="compile">
		<jar destfile="${jar.dir}/${jar.file}.jar" basedir="${classes.dir}">
			<manifest>
				<attribute name="Main-Class" value="${main-class}"/>
                <attribute name="Class-Path" value="${lib.dir}/resources.jar"/> 
			</manifest>
		</jar>
	</target>
	
	<!-- run the application -->
	<target name="run" depends="jar"> 
		<java jar="${jar.dir}/${jar.file}.jar" fork="true"/> 
	</target> 
	
	<!-- run all tasks except cleanup -->
	<target name="all" depends="run"/>
	
	<!-- clean up all files generated by the build -->
	<target name="clean" depends="init"> 
		<delete dir="${build.dir}"/> 
		<delete dir="${classes.dir}"/>
		<delete dir="${apidoc.dir}"/>
		<delete dir="${jar.dir}"/>
	</target> 

 </project>